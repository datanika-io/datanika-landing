---
const steps = [
  {
    number: "01",
    title: "Connect",
    description: "Add your data sources — databases, APIs, file uploads, Google Sheets — and configure destination warehouses.",
    icon: `<svg class="h-8 w-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"/></svg>`,
  },
  {
    number: "02",
    title: "Build",
    description: "Create data uploads with dlt and SQL transformations with dbt. Build staging, intermediate, and mart layers.",
    icon: `<svg class="h-8 w-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"/></svg>`,
  },
  {
    number: "03",
    title: "Run",
    description: "Schedule pipelines with cron, monitor runs in real-time, review logs, and get notified on failures.",
    icon: `<svg class="h-8 w-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>`,
  },
];
---

<section id="how-it-works" class="relative py-24">
  <div class="mx-auto max-w-7xl px-6">
    <div class="text-center">
      <h2 class="text-3xl font-bold text-white sm:text-4xl">
        How it <span class="gradient-text">works</span>
      </h2>
      <p class="mx-auto mt-4 max-w-2xl text-lg text-slate-400">
        Get your data pipeline running in three simple steps.
      </p>
    </div>

    <div class="mt-16 grid gap-8 md:grid-cols-3">
      {steps.map((step, i) => (
        <div class="relative text-center">
          {/* Connecting line (hidden on last item and on mobile) */}
          {i < steps.length - 1 && (
            <div class="absolute top-12 left-[calc(50%+3rem)] hidden h-px w-[calc(100%-6rem)] bg-gradient-to-r from-violet-500/40 to-cyan-400/40 md:block" />
          )}

          <div class="relative mx-auto mb-6 inline-flex h-24 w-24 items-center justify-center rounded-2xl border border-slate-800 bg-slate-900">
            <span class="absolute -top-3 -right-3 rounded-full bg-gradient-to-r from-violet-500 to-cyan-400 px-2.5 py-0.5 text-xs font-bold text-white">
              {step.number}
            </span>
            <div class="text-violet-400">
              <Fragment set:html={step.icon} />
            </div>
          </div>

          <h3 class="mb-3 text-xl font-semibold text-white">{step.title}</h3>
          <p class="mx-auto max-w-xs text-sm leading-relaxed text-slate-400">{step.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
