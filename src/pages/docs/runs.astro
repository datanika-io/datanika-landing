---
import DocsLayout from "../../layouts/DocsLayout.astro";

// "Stopping a run" — datanika-core#657, SPEC_RUN_CANCELLATION D3a. Kept up here, not in an HTML
// comment, because a template comment is served to visitors (tests/compliance-claims.test.ts).
//
// The two bold sentences in that section are the product's own words, and they must stay word for
// word the same as the in-app Cancel dialog and the API's cancel response: core keeps them in
// datanika/services/run_cancellation.py, and tests/cancellation-wording.test.ts holds this page and
// /api/reference to them. Change them in core first.
//
// Read against core master on 2026-09-22: a run cancelled before its engine starts never runs and
// records 0 rows (the pre-flight checkpoint, in production since 2026-09-17); a run already inside
// its engine call is not interrupted and still ends cancelled. The sentences change on the day core
// can interrupt an engine call (the spec's §7.2 "2b").
//
// ✅ Done 2026-09-23: the two things this comment used to defer are shipped and described here.
// "Today this is API-only" is gone (the Cancel control, core PR #1510) and the statuses table
// carries `cancelling` (core PR #1504) — both verified on core `master`, not on `dev`, because
// this page describes production. The control's own words (Stop this run? / Stop this run /
// Keep running / Stopping…) are core's `i18n/en.json`, and `tests/cancellation-wording.test.ts`
// holds this page to them.
//
// 🚨 STILL IN FORCE: do not restore "cancelling early costs less". A cancelled run is billed for
// what it processed, and for a run already inside its engine call that is all of it. That
// sentence becomes true only when a cancel can interrupt an engine call — the same §7.2 "2b"
// condition as the two bold sentences above.
---

<DocsLayout title="Runs & Monitoring">
  <h1>Runs & Monitoring</h1>
  <p class="text-lg text-slate-400">Track execution history, logs, and errors for all your data jobs.</p>

  <h2>Overview</h2>
  <p>A <strong>run</strong> is a single execution of an upload, transformation, or pipeline. Every time a job is triggered — manually or by a schedule — a run record is created to track its progress, outcome, and logs.</p>

  <h3>Properties</h3>
  <table>
    <thead><tr><th>Field</th><th>Description</th></tr></thead>
    <tbody>
      <tr><td><code>target</code></td><td>The upload, transformation, or pipeline that was executed</td></tr>
      <tr><td><code>status</code></td><td>Current state of the run (see below)</td></tr>
      <tr><td><code>started_at</code></td><td>When execution began</td></tr>
      <tr><td><code>finished_at</code></td><td>When execution completed (or failed)</td></tr>
      <tr><td><code>rows_loaded</code></td><td>Number of rows processed (for uploads)</td></tr>
      <tr><td><code>logs</code></td><td>Full execution logs (streamed in real-time)</td></tr>
      <tr><td><code>error_message</code></td><td>Error details if the run failed</td></tr>
    </tbody>
  </table>

  <h2>Statuses</h2>
  <table>
    <thead><tr><th>Status</th><th>Description</th></tr></thead>
    <tbody>
      <tr><td><code>pending</code></td><td>Run is queued, waiting for a worker</td></tr>
      <tr><td><code>running</code></td><td>Execution is in progress</td></tr>
      <tr><td><code>success</code></td><td>Run completed without errors</td></tr>
      <tr><td><code>failed</code></td><td>Run encountered an error</td></tr>
      <tr><td><code>cancelling</code></td><td>A stop was requested; work already in progress is running to its end</td></tr>
      <tr><td><code>cancelled</code></td><td>Run was manually cancelled</td></tr>
    </tbody>
  </table>

  <h2>Run History</h2>
  <p>The run history view lets you filter and browse all past executions:</p>
  <ul>
    <li>Filter by <strong>status</strong> (success, failed, etc.)</li>
    <li>Filter by <strong>target type</strong> (upload, transformation, pipeline)</li>
    <li>Filter by <strong>date range</strong></li>
    <li>View <strong>streaming logs</strong> in real-time for running jobs</li>
  </ul>

  <h3>Example</h3>
  <pre tabindex="0"><code>Run #1247
Target: Upload → "Sync Stripe data"
Status: success
Started: 2026-03-10 02:00:03 UTC
Finished: 2026-03-10 02:03:47 UTC
Duration: 3m 44s
Rows loaded: 15,832</code></pre>

  <h2>How to monitor and manage runs</h2>
  <ol class="list-decimal pl-6 space-y-2">
    <li>Navigate to <strong>Runs</strong> in the sidebar to see all execution history</li>
    <li>Use the <strong>Status</strong> filter to show only <code>success</code>, <code>failed</code>, <code>running</code>, or <code>pending</code> runs</li>
    <li>Use the <strong>Target type</strong> filter to narrow down to uploads, transformations, or pipelines</li>
    <li>Click on a run row to expand <strong>Logs</strong> — logs stream in real-time for running jobs</li>
    <li>For failed runs, check the <strong>Error</strong> column for a summary and the logs for the full stack trace</li>
    <li>To stop a run that is still <code>pending</code> or <code>running</code>, click <strong>Cancel</strong> on its row and confirm — see <a href="#stopping-a-run">Stopping a run</a></li>
    <li>To re-run a failed job, go back to the source (upload/transformation/pipeline) and click <strong>Run</strong></li>
  </ol>

  <h2 id="stopping-a-run">Stopping a run</h2>
  <p>
    You can stop a run that is <code>pending</code> or <code>running</code>, from the app or over the
    API. A run that has already finished returns <code>409</code> — there is nothing left to cancel.
  </p>

  <h3>From the Runs page</h3>
  <p>
    Open <strong>Runs</strong> in the sidebar. A row that can still be stopped carries a
    <strong>Cancel</strong> button. <strong>Editors and above</strong> see it; viewers do not.
  </p>
  <ol class="list-decimal pl-6 space-y-2">
    <li>Click <strong>Cancel</strong> on the run's row.</li>
    <li>
      A confirmation opens, titled <strong>Stop this run?</strong>. It names the run by id and
      target so you can check you have the right one, and states what stopping does.
    </li>
    <li>Choose <strong>Stop this run</strong> to go ahead, or <strong>Keep running</strong> to back out.</li>
    <li>
      The row then shows a <strong>Stopping…</strong> badge. A run that had not started its work
      becomes <code>cancelled</code> immediately; one already working stays
      <code>cancelling</code> until the work it is doing ends.
    </li>
  </ol>

  <h3>Over the API</h3>
  <pre tabindex="0"><code>POST /api/v1/runs/&#123;id&#125;/cancel</code></pre>
  <!-- `&#123;id&#125;` rather than literal braces: Astro parses `{...}` in markup as a JS
       expression, so `{id}` fails the build with `id is not defined` — and would render a
       variable's VALUE instead of the path parameter if one happened to be in scope. -->
  <p>
    The <code>200</code> response is the updated run plus a <code>notice</code> field carrying the
    same sentences the app shows. Its <code>status</code> is <code>cancelled</code> for a run that
    had not started, or <code>cancelling</code> for one still working. See
    <a href="/api/reference">the API reference</a>.
  </p>

  <h3>What cancelling does, and what it does not do</h3>
  <p>
    <strong>A run that has not started its work yet stops before anything is read or written. Work
    already in progress cannot be interrupted: it runs to the end, and the run is then marked
    cancelled. Data already written to your destination stays there, so a re-run that appends
    loads those rows again.</strong>
  </p>
  <p>
    <strong>You are billed for what was processed before the run stopped.</strong> For a run whose work
    had already started, that is all of it.
  </p>
  <ul>
    <li>
      <strong>A run cancelled before it started</strong> records <strong>0</strong> rows and no start
      time, and its log says the engine never ran.
    </li>
    <li>
      <strong>Nothing is rolled back.</strong> Rows written to your destination stay there, and the run's
      <strong>Rows</strong> column counts them. If you need the destination clean, drop or truncate the
      table yourself — we will not do it on your behalf.
    </li>
  </ul>
  <p>
    In short: <strong>cancelling keeps a waiting run from starting; it does not stop one that is already
    working.</strong> To keep a scheduled run from happening at all, pause or delete its schedule before it
    fires.
  </p>

  <h2>Error Handling & Retries</h2>
  <p>When a run fails:</p>
  <ul>
    <li>The full error message and stack trace are captured in the run logs</li>
    <li>The run status is set to <code>failed</code></li>
    <li>If notifications are configured, team members are alerted</li>
    <li>Failed runs can be retried manually from the UI</li>
  </ul>
</DocsLayout>
