# SQL Server (MSSQL) setup-guide screenshots

Referenced from `src/content/connectors/mssql.md`. See PostgreSQL README for the canonical 5-shot capture checklist pattern.

## Required: 01-credentials.png, 02-add-connection.png, 03-configure.png, 04-first-run.png, 05-schedule.png

The MSSQL guide is currently destination-focused. `02-add-connection.png` should show the 5 connection fields from `/connectors/mssql/` (Host, Port 1433, Database, Username, Password) plus the auto-populated Connection Name, with both **Test Connection** and **Create Connection** buttons visible.

Dark theme, 1600 px, pngquant. Redact secrets (password, hostnames of production SQL Server instances).
